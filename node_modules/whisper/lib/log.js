(function(){
  var Logger;
  Logger = require('lochness').Logger;
  module.exports = Logger.make().derive({
    fatal: function(message){
      Logger.fatal.call(this, message);
      return process.exit(1);
    }
  });
}).call(this);
